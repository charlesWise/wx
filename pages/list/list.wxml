<!--list.wxml-->
<view class="container list">
  <block wx:for="{{lists}}" wx:for-item="item" wx:key="*this">
    <view bindtap="goDetail">
      <text class="item">{{index + 1}}. {{item}}</text>
    </view>
  </block>
</view>
